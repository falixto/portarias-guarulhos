name: Extrair Portarias Guarulhos

on:
  workflow_dispatch:

jobs:
  executar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Instalar dependências
        run: |
          pip install requests beautifulsoup4 pdfplumber

      - name: Executar extração
        run: |
          python extrair_portarias.py

      - name: Publicar CSV
        uses: actions/upload-artifact@v4
        with:
          name: portarias-guarulhos
          path: portarias_garulhos.csv
